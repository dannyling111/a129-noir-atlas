# WP52 · 修复旋转地面闪烁

状态：本地修复、构建、测试和证据齐备；线上发布未执行。

输入：用户两张 5.1 旋转场景截图、原 5.1 HTML 与源码 ZIP。

根因：办公楼和庭院底座与表层顶面共面重叠；细微高差的道路叠层在远景也不可靠。

改动范围：worlds.js、renderer.js、camera-3d.js、版本标签与新增专项测试。未改人物动作、语音实现或剧本数据契约。

必须运行：python build.py；node tests/test_motion.js；浏览器、纯净渲染、深度稳定性三套 Python 检查。对照原 5.1，不能删除负对照，也不能用关闭全部投影换取通过。

发布闸：必须实际接入授权仓库、读取仓库约束和既有路径、验证 Pages 输出，方可报告线上完成。当前没有这类证据。
